spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          -  namespace: springkube-ns
             name: spring-kube-map
      reload:
        enabled: true
        mode: event